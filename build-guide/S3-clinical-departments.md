# S3 임상 부서 — LIS · PACS · 장비 인터페이스 · 부서별 대외 보고

> ⚠️ 리허설 전 초안 — 새 설치본으로 따라가 보기 전입니다. `확인 필요(따라가기)` 표시는 따라가기에서 확정합니다. · 기준 2026-09-11 · [가이드 목차](README.md)

## ① 목적과 완료 조건

검사(LIS)와 영상(PACS)을 HIS 에 붙이고, 검사 장비 · 영상 장비 · 라벨 프린터를 연결합니다. 이미 쓰는 PACS 가 있으면 표준 프로토콜(DICOM · HL7 v2)로 연결하는 선택도 있습니다. 약제 · 감염관리 · 암등록 · 응급 같은 **부서별 대외 보고**의 준비 상태도 이 단계에서 확인합니다.

완료 조건:

- [ ] LIS 가 설치 · 검증 스크립트를 통과했고, HIS 의 검사 오더를 받아 온다
- [ ] PACS 가 떠 있고, HIS 영상 오더가 PACS 워크리스트에 등록된다
- [ ] 🔴 LIS · PACS 저장소의 **설치본 주소 기본값**을 자기 기관 값으로 바꿨다
- [ ] 🔴 PACS 의 **영상 자동 선별(코드 기본값 켜짐)을 껐다** — 켜는 것은 [S6](S6-ai.md) 결정 뒤
- [ ] 영상 장비가 워크리스트를 조회하고 영상을 보낸다
- [ ] 부서별 대외 보고(마약류 · 감염병 · 암등록 · 진료정보교류 등)의 **대체 경로**를 정했다(전송 모듈 미구현)

## ② 설치

### LIS

| 항목 | 내용 |
|---|---|
| 구현 상태 | `파일럿` |
| 설치 | 준비된 호스트에서 **설치 스크립트 한 번**으로 올라오도록 돼 있습니다(LIS 저장소 `infra/install/install.sh`). 스크립트는 비밀값을 무작위로 만들고, 마이그레이션 적용을 확인하고, 백업 크론을 등록하고, 관리자 초기 비밀번호를 한 번만 출력합니다. 기존 환경 파일이 있으면 덮어쓰지 않고 멈추며, http 주소는 거부합니다 |
| 검증 | 검증 스크립트(`infra/install/verify.sh`)는 "떴다"가 아니라 "쓸 수 있다"를 봅니다(무인증 차단 · API 문서 비노출 · 백업 크론 등록) |
| 시드 | `starter` · `demo` · `all` 중에서 고릅니다. **운영 설치는 `starter`** 이고, 데모 환자 · 데모 계정은 `demo` 에만 있습니다. 지정은 **환경 변수 `SEED_PROFILE`** 로 합니다 — `SEED_PROFILE=starter npm run db:seed`. 🔴 **지정하지 않으면 기본값이 `all` 입니다**(기존 개발·파일럿 동작을 보존하려고 그렇게 두었습니다). 운영 설치에서 빠뜨리면 **데모 계정과 데모 환자가 함께 들어갑니다** — S8 개시 전에 지우는 대상입니다 |
| 같은 호스트 | 다른 시스템의 컨테이너가 같은 호스트에 있으면 compose 프로젝트 이름을 고정해 충돌을 막습니다 |
| 따라가기(2026-09-13) | ✅ 빌드 · DB · API 기동 · **마이그레이션 73개 자동 적용** · `/api/v1/health`(`db up`) · web 까지 설계대로 올라옵니다. 🔴 다만 **설치 스크립트가 4단계에서 멈춥니다** — 마이그레이션 확인을 `docker logs … \| grep -q` 로 하는데, `pipefail` 아래서 `grep -q` 가 먼저 끝나 앞 명령이 중단 신호로 죽고 **실제로는 적용됐는데 "로그를 찾지 못했다"로 판정**합니다. 다시 돌리면 `.env` 가 이미 있어 거부되므로, **남은 단계(시드 → web 기동 → 백업 일정)를 스크립트와 같은 명령으로 손으로** 진행합니다. 🔴 또 **운영 이미지 안에서는 시드가 돌지 않습니다**(`ts-node` 가 `.ts` 를 처리할 설정 파일이 이미지에 없음) — 시드를 건너뛰면 **사용자 0명**으로 떠 로그인할 수 없습니다. API Dockerfile 의 빌드 단계를 이미지로 만들어(`docker build --target build`) 같은 DB 에 시드를 돌리면 `starter`(관리자 1 · 환자 0)가 들어갑니다 |

설치 스크립트를 쓰지 않고 손으로 올릴 때의 순서는 저장소의 작업 스크립트 이름으로 확인됩니다(기준 커밋 `package.json`).

```sh
npm run docker:up                        # 컨테이너 기동(infra/docker/docker-compose.yml)
npm run db:migrate                       # 마이그레이션 — 🔴 운영은 prisma migrate deploy 쪽을 씁니다
SEED_PROFILE=starter npm run db:seed     # 운영 설치용 시드(기본값 all 을 반드시 덮어씁니다)
```

- **관리자 초기 비밀번호**는 환경 변수로 주지 않으면 **무작위로 만들어 한 번만 출력**합니다. 출력된 값을 놓치면 다시 볼 수 없습니다.
- `starter` 에 들어가는 것은 역할 · 관리자 계정 한 개 · 검사 마스터 · 참고치 · 코드 매핑 · QC 기준값입니다. **직무 계정(검사자 · 검증자 등)은 `demo` 쪽**이라 운영 설치에는 들어가지 않습니다 — 실제 직원 계정은 기관이 만듭니다.
- 근거: LIS 기준 커밋의 시드 스크립트 머리말과 작업 스크립트 선언 · 2026-09-12 확인. 설치 스크립트의 인자(스크립트 머리말 기준): `--site-code <코드>` · `--origin https://<도메인>`(필수) · 선택 `--trust-proxy-hops`(기본 1) · `--with-demo`(평가 · 교육용 합성 데이터 — **운영 설치에는 쓰지 않음**) · `--no-backup-cron`(백업 일정을 시스템 스케줄러에서 따로 관리할 때). 사전 점검은 docker · compose v2 · **디스크 여유 10GB 이상** · 포트 점유이고, 스크립트는 **DB 에 데이터가 있으면 시드를 건너뛰고**, OS · Docker 설치 · 방화벽 · TLS 인증서 · DNS · 백업 저장소는 하지 않습니다(호스트 준비 몫).

### PACS

| 항목 | 내용 |
|---|---|
| 구현 상태 | `통합` |
| 구성 | Orthanc · PostgreSQL · Redis · 백엔드 API · 비동기 작업자(worker · beat) · 워크리스트(MWL/MPPS) 서버 · HL7 수신기 · 관리 화면 · 웹 뷰어 · nginx(유일한 공개 진입점). 모니터링은 프로필로 켭니다 |
| 웹 뷰어 | **먼저 빌드합니다.** 빌드 스크립트는 **`werub-viewer/build.sh`** 이고, **OHIF `v3.12.0` 소스를 받아** 로고 · 설정 · 한국어 번역 · 확장을 얹어 정적 파일을 만드는 7단계입니다. 🔴 **기존 OHIF 설치를 건드리지 않고 새로 만듭니다.** 소스를 받는 단계가 있으므로 **격리 구간에서는 미리 받아 반입**합니다(S0) |
| 라이선스 | Orthanc 코어 GPL-3.0-or-later · DICOMweb · PostgreSQL 플러그인 AGPL-3.0-or-later · OHIF · Cornerstone3D MIT(원 고지 포함 필요). 이미지 안의 실제 판본과 켜지는 플러그인 목록은 확인이 남아 있습니다([THIRD_PARTY.md](../THIRD_PARTY.md)) |

PACS 저장소 README 의 **Quick Start → Installation** 이 안내하는 명령(기준 커밋 표기):

```sh
cp .env.example .env     # 환경 파일을 만들고 기관 값으로 고칩니다
docker compose up -d     # 전체 서비스 기동
docker compose ps        # 컨테이너 상태 확인
```

뷰어는 위 기동 **전에** `werub-viewer/build.sh` 로 만들어 둡니다.

- **운영용 compose 파일이 따로 없습니다** — 기준 커밋의 compose 는 `docker-compose.yml` 한 벌이고(그 밖에는 AI 모델 서버용 추가 파일 하나), 운영과 개발의 차이는 `.env` 값으로 냅니다.
- **모니터링은 `docker compose --profile monitoring up -d`** 로 켭니다. 이 프로필에 묶인 것은 Prometheus · Alertmanager · PostgreSQL exporter · Redis exporter · node exporter · Grafana 여섯입니다. 프로필 없이 올리면 이 여섯은 뜨지 않습니다.
- **설치 스크립트** `scripts/install/install.sh`(멱등): 사전 점검 → `.env` 무작위 시크릿 → 빌드 → 기동 → 첫 관리자(비밀번호 1회 출력) → 기관 시드 → 검증(C-ECHO 포함).
- 🔴 **따라가기(2026-09-14)에서 새 설치가 스크립트만으로는 끝나지 않았습니다.**
  - 기준 커밋 compose 의 Orthanc 이미지 태그를 **레지스트리에서 받을 수 없어** 가장 가까운 판으로 바꿔 진행했습니다(판본 결정은 PACS 담당 확인 뒤).
  - Orthanc 가 **DICOM TLS 를 꺼 둬도 설정에 적힌 인증서 파일이 없으면 멈췄습니다** — `scripts/install/gen-dicom-tls.sh <호스트>` 로 파일만 만들어 두면 뜹니다(TLS 는 계속 꺼짐).
  - Orthanc 헬스체크가 이미지에 없는 도구(`curl`)를 써서 늘 실패하고, 그래서 nginx 가 뜨지 않았습니다(바꾼 이미지 판의 영향일 수 있음).
  - **새 DB 에 기본 테이블을 만드는 단계가 스크립트 · 이미지 어디에도 없어** 첫 관리자 만들기에서 멈췄습니다. 따라가기에서는 백엔드 모델로 테이블을 만든 뒤 스크립트를 다시 돌려 끝냈습니다. 새 기관용 스키마 생성 절차는 `확인 필요(따라가기)`.
  - 그 뒤로는 API · 뷰어 · DICOMweb · 관리 화면 · DICOM C-ECHO 까지 설계대로 응답했습니다.

### 장비 · 주변기기

- **영상 장비(모달리티)**: PACS 가 워크리스트 조회(C-FIND) 응답 · 검사 진행 상태(MPPS) · 영상 수신(C-STORE)을 맡습니다(`구현·미검증`). 우리 쪽 **AE 타이틀 기본값은 `OPENPACS`** 이고, **연결할 장비 목록(`DicomModalities`)은 비어 있는 채로 출고됩니다** — 기관이 장비마다 AE 타이틀 · 주소를 등록합니다(Orthanc 설정 파일 `docker/orthanc/orthanc.json`). 지원하는 SOP 클래스 · 전송 구문 · 문자 집합은 저장소의 **DICOM 적합성 선언**에 있습니다. 장비 쪽(제조사 콘솔)에서의 등록 절차는 장비마다 달라 `확인 필요(따라가기)`.
- **외부 PACS · 기관 간 영상 교환**: 원격 DICOM 노드 조회 · 가져오기 · 보내기와 IHE XDS-I.b 제출이 있습니다(`구현·미검증`).
- **검사 장비(분석기) → LIS**: 결과 자동 수집은 `미구현` 입니다(⑥).
- **라벨 프린터(ZPL) · WebUSB 장치**: Go-Live 에서 자가신고로 추적합니다. 연결 절차는 `확인 필요(따라가기)`.

## ③ 설정

### LIS

- 설치 뒤 기관이 넣는 사이트 값(외부 통보 채널 · 오프사이트 백업 경로 등)은 LIS 의 **개시 전환 센터** 체크리스트로 입력 순서와 완료를 확인합니다.
- HIS 와의 검사 오더 경로는 **FHIR 폴링**(LIS 가 HIS 의 검사 오더를 5분 주기로 증분 조회)입니다. LIS 는 **SMART Backend Services(client_credentials)** 로 HIS 에 붙습니다 — HIS `SMART 클라이언트`(`/admin/smart-clients`)에 LIS 를 클라이언트로 등록하고, 받은 값을 LIS 운영 환경 파일에 넣습니다.
  - LIS 쪽 키(운영 환경 예시 기준): `HIS_FHIR_BASE` · `HIS_FHIR_TOKEN_URL` · `HIS_CLIENT_ID` · `HIS_CLIENT_SECRET` · `HIS_FHIR_SCOPES` · `HIS_ORDER_POLL`(자격증명을 넣은 뒤 켬) · `HIS_REFLEX_POLL` · `HIS_REFLEX_RETRY` · `HIS_INTEGRATION_BASE` · `HIS_INTEGRATION_KEY`.
  - 스코프를 비워 두면 코드 기본값 `system/DiagnosticReport.write` · `system/Observation.write` · `system/ServiceRequest.read` · `system/Patient.read` 를 요청합니다. HIS 에 등록할 때 이 네 가지만 허용합니다.
  - 🔴 `HIS_FHIR_TOKEN_URL` 을 비우면 코드 기본값이 **특정 설치본의 토큰 주소**입니다. 반드시 채웁니다.
  - **따라가기(2026-09-14 · 새 설치본끼리) — `검증됨`**: HIS 에 LIS 를 SMART 클라이언트로 등록 → LIS 운영 환경 파일에 `HIS_FHIR_BASE`(HIS 의 `/fhir/R4`) · `HIS_FHIR_TOKEN_URL`(HIS 의 `/oauth2/token`) · `HIS_CLIENT_ID` · `HIS_CLIENT_SECRET` · `HIS_ORDER_POLL=on` → 다음 5분 주기에 **HIS 검사 오더가 LIS 오더로 적재**되고(HIS 오더 번호 그대로 · 검사 항목 · `RECEIVED`), 환자 이름도 HIS 에서 읽어 옵니다. 틀린 시크릿 · 허가하지 않은 스코프 · 토큰 없음은 401, 허가 범위 밖 리소스 읽기는 403 으로 막힙니다.
  - 🔴 **새 설치본에서는 대부분의 검사 오더가 LIS 로 넘어가지 않습니다.** HIS 기본 시드의 검사 코드 15개 가운데 **LIS `starter` 코드 매핑에 있는 것은 3개**(갑상선기능 · 일반뇨 · 혈액배양)뿐이라, CBC · 전해질 · CRP · 간기능 · 신장기능 · 혈당 · 지질 · HbA1c · 혈액응고 오더는 LIS 가 **적재하지 않고 대기열에 보류**합니다 — LIS 는 이것을 조용히 넘기지 않고 **"미적재 HIS 오더 N건 대기 — 코드매핑 보정 후 재적재 필요(접수 누락)"** 경보를 냅니다. **개시 전에 LIS 의 HIS 검사코드 카탈로그 반입과 코드 매핑을 먼저 끝냅니다.**
  - HIS 기본 시드의 검사 오더 12건은 **처방 코드가 연결돼 있지 않아 FHIR 에 노출되지 않습니다**(코드 없는 오더는 LIS 가 처리할 수 없어 HIS 가 일부러 뺌). 연결 시험에는 코드가 있는 오더를 새로 내야 합니다.
- Reflex 추가검사 오더의 전송 방식 키 `HIS_ORDER_TRANSPORT` 가 있습니다. HL7(ORM) 쪽 경로는 `미구현` 이므로 FHIR 경로를 씁니다.
- HL7 MLLP 구간은 원내 폐쇄망인지 확인하거나, 상대 시스템과 전송 구간 보호(TLS · 전용 VPN)를 합의합니다(LIS 저장소 문서).
- 지표 엔드포인트의 접근 범위는 앞단 프록시에서 기관 망 정책에 맞춰 좁힙니다.

### PACS

- 🔴 **AI 기본값**(2026-09-11 코드 확인): 판독문 자동 초안 · 드리프트 자동 격리는 기본 꺼짐이고, **영상 자동 선별은 기본 켜짐**입니다. 설치할 때 끄고, 기관 결정 뒤에 켭니다 → [S6](S6-ai.md). 설정 키는 `screening_enabled`(환경 변수 `SCREENING_ENABLED`).
- 🔴 **설치본 주소**: 코드 기본값 가운데 특정 설치본의 주소가 들어 있는 설정(프레임 캐시 예열 기본 주소 등)이 있습니다. 자기 기관 주소나 내부 주소로 바꿉니다. 전체 목록은 [바꿔야 할 코드 기본값 — PACS](replace-list.md#pacs)(`주소` 31파일 · 기준 커밋).
- **HL7 수신 허용 대역**: 앱의 허용 대역 설정과 호스트 방화벽 허용 대역을 **함께** 맞춥니다. 저장소의 통합 테스트 스크립트로 확인합니다.
- **선택 기능**: IHE ATNA 감사 송신 · 모달리티 구간 DICOM TLS 는 기본 꺼짐입니다.
- **백업**: 오프사이트 대상은 설정해야 켜집니다. 암호화 백업의 패스프레이즈는 서버 밖에도 보관합니다.
- **앞단 프록시**: PACS 앞에 역방향 프록시를 한 단 더 두는 구성이면, PACS 저장소의 해당 운영 기록(v13.48)을 먼저 읽고 구성합니다.

### HIS 쪽

- HIS 는 영상 오더를 만들 때 PACS 워크리스트에 자동 등록하고, 실패분을 다시 등록합니다.
- PACS 는 HIS DB 를 **읽기 전용**으로 조회해 워크리스트를 동기화합니다(관리자 요청 시). 이 연결 문자열은 PACS 환경 변수 **`HOSPITALRUN_DB_URL`** 이고, 조회문은 코드에 "검증된 읽기 전용 조회"로 적혀 있습니다(`SELECT` 만). HIS DB 에 이 용도의 **읽기 전용 역할을 만드는 스크립트는 두 저장소의 기준 커밋에서 찾지 못했습니다** — 역할과 권한 범위(어느 테이블까지)는 기관 DBA 가 만들고, 그 SQL 은 `확인 필요(따라가기)`.
- 영상 바이트 중계(WADO)는 배포 구성(주소 · 앞단 프록시)에 따라 달라져 코드만으로 판정하지 못했습니다(`판정 불가`).

## ④ 사람이 정할 것 (2)

| 키 | 층 | 개시 관계 | 무엇을 정하나 |
|---|---|---|---|
| `policy.labResult.staffNotify` | 원내 위원회 | 개시 뒤 가능(30일) | 직원 검사 결과 알림을 살릴 것인가 — 기록하면 시스템이 `notification.staffLabResult.enabled` 를 반영합니다 |
| `policy.vitalsSourceMeta` | 원내 위원회 | 개시 뒤 가능(30일) | 활력 출처 메타 · 회복실 통합을 위한 스키마 변경을 승인할 것인가(배포로 반영) |

관련 결정: 약제 스키마 · 처방전 유효기간은 [S1](S1-core-his.md), 위급 검사값의 환자 알림은 [S2](S2-patient-access.md), 영상 AI 는 [S6](S6-ai.md).

결정 등록부 밖에서 기관이 정할 것: 부서별 대외 보고의 대체 경로와 담당 · 기존 PACS 를 계속 쓸지 · 보존 기간이 지난 검사 데이터의 파기 시점(LIS 는 기관이 보존 기간을 정한 뒤 켭니다).

## ⑤ 확인

| 확인 화면 | 무엇을 보나 |
|---|---|
| `/admin/integration-gates` | LIS Reflex 연동 게이트의 실제 결재 기록 |
| `/admin/outbound-channels` | 대외 전송 채널(`NIMS` · `KIDS` · `KCDC` · `KONIS` · `KCCR` · `HIE_HIGHWAY`)의 구현 여부 |
| `/admin/config` | 119 사전통보 설정 |
| LIS 개시 전환 센터 | LIS 사이트 값의 입력 순서와 완료 |
| PACS 관리 화면 · 상태 점검 | 서비스 상태 · 워크리스트 |

이 단계를 닫는 개원 단계 항목 (8):

| 키 | 항목 | 국가 | 확인 방식 |
|---|---|---|---|
| `open.license.narcotics` | 마약류취급자 허가 — 마약류 통합관리시스템 보고와 이어짐 | KR | 사람 기록 |
| `open.license.radiation` | 진단용 방사선 발생장치 신고 · 검사 | KR | 사람 기록 |
| `open.ae.fanr` | FANR 방사선 취급 면허 — 🔴 관할 보건당국이 아니라 연방 FANR 소관 | AE | 사람 기록 |
| `open.ae.rpo` | 방사선방호책임자 지정 · 방사선방호프로그램 | AE | 사람 기록 |
| `open.workforce.infectionOfficer` | 감염관리 담당자 지정 | KR · AE | 사람 기록 |
| `open.ae.narcoticInCharge` | 마약류 담당자 지정 | AE | 사람 기록 |
| `open.ae.hie` | 보건정보교류(HIE) 연동 — 🔴 면허 조건으로 요구됩니다 | AE | 사람 기록 |
| `open.ae.tatmeen` | 의약품 추적(Tatmeen) 연동 | AE | 사람 기록 |

이 단계를 닫는 Go-Live 항목 (11):

| 키 | 항목 | 판정 | 개시 차단 |
|---|---|---|---|
| `integ.lis` | LIS Reflex 양방향 오더 컷오버 — 연동 게이트의 실제 결재 기록(개발 시드 결재는 세지 않음) | 실검증 | 🔴 예 |
| `integ.pacsCutover` | PACS 컷오버(작업 창 확정 · 당일 병리오더 검증) | 자가신고 | 아니오 |
| `integ.nimsKids` | 마약류 NIMS · 부작용 KIDS 실전송 | 실검증(구현 여부) | 🔴 예 |
| `integ.kcdc` | 법정감염병 신고 전송 | 실검증(구현 여부) | 🔴 예 |
| `integ.konis` | 의료관련감염 보고 전송 | 실검증(구현 여부) | 🔴 예 |
| `integ.kccr` | 암등록 전송 | 실검증(구현 여부) | 🔴 예 |
| `integ.hie` | 진료정보교류(HIE) 문서 전송 | 실검증(구현 여부) | 🔴 예 |
| `integ.ems119` | 119 사전통보 개통 | 자가신고 | 아니오 |
| `integ.bloodOrder` | 혈액원 발주 실연계 | 자가신고 | 아니오 |
| `hw.printer` | 라벨 프린터(ZPL) 실기기 연동 | 자가신고 | 아니오 |
| `hw.webusb` | WebUSB 장치 입출력 | 자가신고 | 아니오 |

## ⑥ 아직 안 되는 것과 대체 수단

| 아직 안 되는 것 | 대체 수단 |
|---|---|
| 🔴 **부서별 대외 전송**(마약류 · 부작용 · 감염병 · 의료관련감염 · 암등록 · 진료정보교류) 모듈이 구현돼 있지 않습니다. Go-Live 는 구현 여부를 직접 읽으므로, 사람이 완료로 적어도 **미완으로 남습니다**. **채널별 상태와 그동안의 수기 대체 절차는 `기관 연동`(`/admin/agency-integrations`) 화면에 있습니다** — 채널마다 "그동안 사람이 할 일"(수기 신고 후 접수번호 기록)과 법적 근거를 적습니다(2026-09-12 확인 · [화면 소개](../screens/his.md#기관-연동--안-되는-것을-일정으로-관리한다)) | 각 기관이 정한 기존 신고 경로를 쓰거나 기관이 전송 모듈을 붙입니다. 이 항목들이 개시 차단으로 남는 동안 개시 판단을 어떻게 기록할지는 `확인 필요(따라가기)` |
| 검사 장비(분석기) 결과 자동 수집이 `미구현` 입니다(ASTM 저수준 전송 없음) | 장비와 LIS 사이의 인터페이스 수단을 기관이 따로 준비합니다. 방법은 `확인 필요(따라가기)` |
| HIS ⇄ LIS 의 HL7 v2 대체 경로(결과 ORU · 처방 OML · Reflex ORM)와 Reflex 승인 웹훅이 `미구현` 입니다 | FHIR 경로(`구현·미검증`)와 폴링을 씁니다 |
| PACS → HIS 판독 결과 HL7 송신 · 환자 인구정보 HL7 ADT(PIX Feed) · 환자 포털의 외부 영상 업로드 중계가 `미구현` 입니다 | 판독 결과는 PACS 화면과 판독 서명(S4) 경로로 봅니다. 외부 영상은 원내 반입 절차로 받습니다 |
| 기관 간 영상 교환(XDS-I.b)과 환자 식별 교차 참조(PIX)는 모의 도메인에서만 확인했습니다 | 실제 도메인 연결은 상대 기관과 따로 확인합니다 |
| PACS 는 인허가받은 의료기기가 아닙니다(인허가 대응 문서만 있음) | 임상 사용의 적합성 판단과 인허가는 구축 기관이 합니다 |
| PACS 웹 뷰어 빌드 산출물의 npm 고지 목록이 아직 없습니다 | 뷰어를 다시 빌드하거나 배포하는 기관은 고지 목록을 따로 확인합니다 |
| PACS 코드에 버전 선언이 없어 실행 중인 서비스에서 릴리즈 번호를 읽을 수 없습니다 | 설치본을 기준 커밋 해시로 기록합니다 |
| (UAE) 보건정보교류 · 의약품 추적 연동의 구현 상태 | `확인 필요(따라가기)` |

## ⑦ 흔한 함정

- **HL7 허용 대역을 한쪽만 바꾸는 것.** 앱과 호스트 방화벽 중 한쪽만 바꾸면 연결이 **조용히** 끊깁니다.
- **운영 설치에 `demo` 시드를 쓰는 것.** 운영은 `starter` 입니다.
- **같은 호스트에서 compose 프로젝트 이름이 겹치는 것.** 다른 시스템의 컨테이너와 충돌합니다.
- **PACS 작업자 코드를 고치고 이미지를 다시 빌드하지 않는 것.** 작업자는 코드가 이미지에 들어갑니다.
- **Reflex 전송 방식을 HL7 로 바꾸는 것.** 그 경로는 아직 없습니다.
- **Go-Live 의 `integ.lis` 를 개발 시드 결재로 채우려는 것.** 개발 시드 결재는 세지 않습니다.
- **LIS 버전을 올리며 화면 표기 상수를 빠뜨리는 것.** 화면 표기용 버전 상수가 패키지 버전과 따로 있습니다.
- **백업 패스프레이즈를 서버에만 두는 것.**
