# S1 코어 HIS — 설치 · 기관 정보 · 코드 마스터 · 조직 · 병원 규정

> ⚠️ 초안 — **새 설치본으로 한 번 따라가 본 결과를 반영했습니다**(2026-09-13~16 · 개발 PC 한 대 · arm64 · 8GB 가상 머신 · GPU 없음 · 외부로 나가지 못하는 네트워크). 남은 `확인 필요(따라가기)` 표시는 x86 · GPU 기준 장비나 사람 결정이 필요한 곳입니다. · 기준 HIS v4.18.0(`e9d303984f80`) · 2026-09-11 · [가이드 목차](README.md)

## ① 목적과 완료 조건

생태계의 코어이자 **신원 허브**인 HIS 를 격리 네트워크 안에 세웁니다. 직원 로그인 토큰을 HIS 가 발급하고 다른 시스템이 검증하므로, 이후 모든 단계가 HIS 에 기댑니다.

완료 조건:

- [ ] HIS 웹 · API · 상시 감시자가 떠 있고, 시스템 설정 화면(`/admin/config`)이 열린다
- [ ] 🔴 HIS 저장소의 **설치본 주소 기본값을 자기 기관 값으로 바꿨고**, 격리 상태에서 밖으로 나가는 요청이 없음을 확인했다
- [ ] 화면 · 발급 문서에 **자기 기관명**이 나온다(코드에 고정된 병원명 교체 포함)
- [ ] 🔴 개인식별정보 암호화 키를 새로 만들었다(`security.encryptionKey` 통과)
- [ ] 🔴 정기 암호화 백업이 돈다(`security.dbBackup` 통과)
- [ ] 국가에 맞는 코드 마스터를 반입했다
- [ ] 부서 · 병상 · 직원 · 역할을 등록했다
- [ ] 🔴 요양기관 종별을 정했다(`billing.institutionType`)

기관 기본정보 가운데 인허가에서 나오는 값(요양기관 기호 · 사업자등록번호 · 대표자 등)은 이 단계에서 다 채우지 못할 수 있습니다. 그 값은 [S8](S8-go-real.md) 전에 확정합니다.

## ② 설치

### 구성 요소

| 구성 요소 | 비고 |
|---|---|
| 웹(Next.js 15) · API(NestJS · Prisma) | 공용 패키지가 상태 라벨 · 수치 정책 · 역할 목록의 정본을 가집니다 |
| PostgreSQL 16 · Redis 7 | Redis 판본과 약관 → [S0](S0-prepare.md) |
| 상시 감시자 | API 와 **별도 프로세스**로 돕니다(v4.14.0 부터) |
| 서버 PDF 렌더(browserless v1 이미지) | GPL-3.0-or-later 또는 상용 — 용도 확인([THIRD_PARTY.md](../THIRD_PARTY.md)) |
| 관측(Grafana · Prometheus) · nginx | Grafana 는 AGPL-3.0 계열 |

같은 저장소에 공개 홈페이지와 환자 앱이 있습니다 → [S2](S2-patient-access.md).

### 설치 순서

HIS 저장소 README 의 **Quick Start** 가 안내하는 순서는 다음과 같습니다(명령 이름은 기준 커밋의 README 표기).

```sh
npm install          # 의존성 설치
npm run docker:up    # PostgreSQL + Redis 컨테이너 기동
npm run db:migrate   # 마이그레이션
npm run db:seed      # 시드(가상 병원 데이터)
npm run dev          # 실행 — 개발 실행
```

- 위 명령은 **개발 실행**입니다(`nest start --watch`). 운영 설치본은 명령이 다릅니다 — **빌드**(`npm run build`) → **스키마 반영** → **기동은 빌드 산출물 실행**(`node dist/main`). 프로세스 관리자 · 감시자 구성은 `확인 필요(따라가기)`.
- 🔴 **스키마 반영은 `prisma migrate deploy` 가 아니라 `prisma db push` 입니다**(2026-09-13 따라가기에서 정정 · 이전 판의 이 가이드가 틀렸습니다). 기준 커밋의 `migrations/` 폴더에는 파일이 3개뿐이고, `migrate deploy` 는 그중 한글 검색 인덱스 파일에서 **트랜잭션 안에서는 실행할 수 없다는 오류로 멈춥니다.** 저장소의 배포 스크립트도 `db push` 만 쓰고, 해당 파일 머리말이 "migrations/ 는 수동 적용 전용"이라고 적습니다. 새 DB 에서 확인한 순서:
  1. MRN 시퀀스와 `pg_trgm` 확장 — `npx prisma db execute --file prisma/migrations/00000000000000_create_mrn_sequence/migration.sql --schema prisma/schema.prisma`
  2. `npx prisma db push` — 새 DB 에서 수 초
  3. 한글 검색 GIN 인덱스는 **API 가 기동할 때 스스로 만듭니다**(기동 로그 "pg_trgm GIN indexes ensured").
  - `prisma/manual/` 의 **수동 SQL 110개**(+ 안내 README)를 `db push` 직후 DB 와 대조했습니다(2026-09-13) — **57개는 이미 반영돼 적용할 것이 없고**, **34개는 `db push` 가 만들지 않는 DB 객체**(인덱스 79개 · 🔴 **검진권 이벤트를 고치지 못하게 막는 트리거 1개와 그 함수**)를, **19개는 기준 데이터 · 데이터 보정**(KCD 8차 마스터 · 간호 코드 · 고위험 약물 · 약가 등)을 담고 있습니다. 새 설치에도 뒤의 두 묶음은 필요합니다. 파일마다 적용 순서 · 다시 돌려도 되는지 · 시드와의 선후가 달라, 순서는 `확인 필요(따라가기)`.
  > ⚠️ `prisma migrate dev` 를 운영 DB 에 쓰지 않습니다. 개발용 명령은 스키마가 어긋나면 **데이터베이스를 다시 만들자고 제안**합니다.
- 기본 시드는 **가상 병원 데이터**이고, 특정 기관명이 들어 있습니다. 가상 데이터는 S7 리허설까지 쓰고 S8 에서 격리합니다.
- 🔴 **첫 관리자 계정을 만드는 경로가 기본 시드뿐입니다.** 초기 관리자를 만드는 스크립트나 환경 변수가 없고, 기본 시드는 직원 계정 88개 · 환자 30명을 **공용 비밀번호**로 만듭니다(따라가기 2026-09-13). 시드로 들어가 → 기관 관리자 계정을 만들고 → 시드 계정을 정리하는 순서가 됩니다(정리 스크립트는 없음 → [S8](S8-go-real.md)).
- 🔴 **기본 시드는 결정 없이 기능을 켭니다** → [S6 1단계](S6-ai.md). 시드를 돌린 뒤에는 `/admin/config` 에서 켜진 것부터 확인합니다.
- 시드 스크립트는 **형 검사를 통과하지 못합니다**(지금 스키마에 없는 필드 · 소스 폴더 import). 실행은 형 검사를 끄고(`TS_NODE_TRANSPILE_ONLY=true`) 하면 끝까지 돕니다(따라가기 2026-09-13).
- 운영 환경 파일의 키 이름(설정 예시 파일 기준): `DATABASE_URL` · `REDIS_URL` · `DB_PASSWORD` · `REDIS_PASSWORD` · `AUTH_SECRET` · `AUTH_REFRESH_SECRET` · `ENCRYPTION_KEY` · `PUBLIC_URL` · `NEXT_PUBLIC_API_URL` · `TZ` · `LOG_LEVEL`. 비밀값은 설치 때 새로 만들고 서버의 환경 파일에만 둡니다. 전체 목록은 `확인 필요(따라가기)`.

### 코드 마스터 반입

코드 마스터는 이 저장소에도 소스에도 없습니다. **기관이 배포 기관에서 직접 받아 반입합니다**([THIRD_PARTY.md §4](../THIRD_PARTY.md#4-코드-마스터기준-데이터)).

| 데이터 | 배포 기관(한국) | 비고 |
|---|---|---|
| 약제급여목록 · 약가(의약품 표준코드 포함) · 저가약 대체조제 목록 | 건강보험심사평가원 | 이용 조건 확인 필요 |
| 수가 · 치료재료 코드(EDI 행위 · 검사 코드) | 건강보험심사평가원 · 보건복지부 고시 | 이용 조건 확인 필요 |
| KCD(한국표준질병·사인분류) | 국가데이터처(구 통계청) | **제9차가 2026-01-01 부터 시행 중**이고, 생태계 코드의 KCD 표기는 기준일에 제8차입니다 |
| DUR 금기 정보 · 의약품 개요정보 | 식품의약품안전처(공공데이터포털) | AI Server 도 씁니다 → [S6](S6-ai.md) |
| 법정감염병 목록 | 질병관리청 | |
| LOINC · SNOMED CT | Regenstrief Institute · SNOMED International(국내 배포 센터 등록) | 조건이 있는 라이선스 |

- **반입 화면과 형식**(HIS 기준 커밋 코드 · 2026-09-13 확인): `코드 마스터`(`/admin/codes`)의 **「고시 개정 일괄 반영」** 패널입니다.
  - 종류는 **진단 · 행위(수가) · 약품** 셋, 파일은 **CSV/TSV 본문을 붙여넣기**(헤더 포함 · **8MB 상한** — 넘으면 나눠 올림), 필수 열 `code` · `nameKo` · 선택 열 `category` · `unitPrice`(진단 제외), 그리고 **시행일**(YYYY-MM-DD).
  - 🔴 **미리보기는 아무것도 저장하지 않습니다** — 차이 리포트를 먼저 보고, 사람이 확인한 뒤 반영합니다. 반영할 때 **수가는 새 버전으로 쌓이고, 나머지는 덮어씁니다.**
  - 약품 보험코드 매핑은 `/admin/drug-mapping` 에서 따로 봅니다. 공공 데이터 원본 파일을 이 열 형식으로 바꾸는 방법은 `확인 필요(따라가기)`.
- 국가별로 어떤 코드 마스터를 반입할 수 있는지(한국 · UAE)는 `확인 필요(따라가기)`.
- 다른 시스템에서 들어오는 마스터도 있습니다 — ERP 의 수가 · 비급여 · 재료대 마스터 적재와 약품 코드 매핑([S5](S5-management.md)), LIS 의 검사코드 카탈로그([S3](S3-clinical-departments.md)). 모두 `구현·미검증` 입니다.

### 부서 · 병상 · 직원 · 역할

- 사용자 역할은 14종입니다(임상 4 · 지원 9 · 관리자 1 — 스키마의 역할 열거값 수 · 2026-09-11 확인).
- 기준 커밋에는 직원 관리 · 역할 권한 · 진료과 카탈로그 · 시설 관리 화면(`/admin/staff-management` · `/admin/rbac` · `/admin/dept-catalog` · `/admin/facility`)이 있습니다. 입력 순서와 병상 등록 경로는 `확인 필요(따라가기)`.
- 직원 등록은 뒤 단계의 기반입니다. 그룹웨어(Clinic) · 교육(edu) · ERP 가 HIS 직원 명부를 받아 갑니다([S5](S5-management.md)).

## ③ 설정

### 설정 화면에서 출처 보기

`/admin/config` 는 설정값마다 **DB 에서 온 값 · 코드 기본값 · 미설정**을 구분해 보여 줍니다. "기본값"으로 표시되는 값은 **아직 아무도 정하지 않은 값**입니다. 이 단계에서 기관이 정한 값은 DB 값으로 바꿔 둡니다.

### 기관 정보

| 키 | 뜻 |
|---|---|
| `hospital.name` · `hospital.nameEn` | 기관명 · 영문명 |
| `hospital.address` · `hospital.addressEn` · `hospital.city` | 주소 · 영문 주소 · 소재 도시 |
| `hospital.phone` · `hospital.hours.*` · `hospital.emergency` | 대표 전화 · 진료시간 · 응급실 운영 |
| `hospital.code` · `hospital.bizNo` · `hospital.ceo` · `hospital.license` | 요양기관 기호 · 사업자등록번호 · 대표자 · 면허/허가번호 — **기본값은 개발용 자리표시이거나 공란**입니다(설정 설명 문구) |
| `hospital.country` | 국가(KR · AE) — 결정 `country.institutionProfile` 로 반영 |
| `hospital.defaultUiLocale` | 기관 기본 화면 언어(ko · en · ja · ar) |

### 🔴 코드에 고정된 병원명

HIS 저장소에는 병원명이 **고정 문자열**로 남은 파일이 118개 있습니다(2026-09-11 기준 커밋에서 다시 셈 · 09-10 값과 같음 · 09-10 분해: 웹 68 · 공개 홈페이지 36 · API 11 · 공용 3). 설정값으로 옮기는 작업이 진행 중이며, **끝나기 전까지는 코드를 고쳐야** 자기 병원명이 나옵니다.

- **고칠 파일 목록** → [바꿔야 할 코드 기본값](replace-list.md)(자동 생성 · 기준 커밋에서 다시 셈). 병원명만이 아니라 기관 정보 · 연락처까지 `식별` 로 묶어 HIS 에서 **183파일**이 나옵니다(병원명 118 을 포함). 목록은 경로만 싣고 문구는 싣지 않습니다.
- 파일마다 문자열을 바꿀지, 설정값(`hospital.*`)을 읽게 고칠지는 `확인 필요(따라가기)`.

### 🔴 설치본 주소 교체

11개 저장소 합계 245개 파일([파일 목록](replace-list.md))에 특정 설치본의 주소가 기본값으로 들어 있습니다(문서 제외 · 2026-09-11 기준 커밋). HIS 쪽에서 먼저 볼 키:

| 키 | 어디 |
|---|---|
| `PUBLIC_URL` · `NEXT_PUBLIC_API_URL` | HIS 환경 파일 |
| `ai.server.url` | HIS 설정 → [S6](S6-ai.md) |
| 공개 홈페이지 · 환자 앱의 주소 키 | → [S2](S2-patient-access.md) |

HIS 저장소 안에서 바꿔야 할 **전체 목록**은 [바꿔야 할 코드 기본값](replace-list.md#his)에 있습니다 — HIS `주소` **73파일**(기준 커밋 · 생태계 전체는 [한눈에](replace-list.md#한눈에) 표). 바꾼 뒤에는 격리 상태에서 밖으로 나가려는 요청이 없는지 확인합니다. 확인 방법(로그 · 방화벽 기록)은 `확인 필요(따라가기)`.

### 암호화 키 · 백업 · 인프라

- **개인식별정보 암호화 키**(`ENCRYPTION_KEY`)는 설치 때 새로 만듭니다. Go-Live 관제가 기본값이거나 비어 있는 키를 "준비 안 됨"으로 표시합니다.
- **백업**은 저장소의 백업 셸 스크립트(`infra/scripts/backup.sh`)를 **운영체제의 정기 작업(크론)에 한 줄로 등록**해 돌립니다. 보존 기간은 스크립트 안의 `RETENTION_DAYS`(기본 **30일**)이고, 지난 파일은 스크립트가 지웁니다. 배포 직전 자동 백업도 같은 방식으로 암호화하며, Go-Live 백업 판정은 **정기 백업만** 셉니다.
  - 🔴 **암호화는 조건부입니다.** 암호문(패스프레이즈) 파일이 있을 때만 암호화본을 만들고, 없으면 **평문 백업만 남습니다.** Go-Live 의 백업 판정은 **암호화본이 만들어진 시각**을 읽으므로, 평문만 있으면 화면이 `준비 안 됨` 으로 말합니다 — 이것이 정상 동작입니다 → [취지 1](../overview/02-principles.md).
  - 🔴 **크론 시각은 서버 시간대를 탑니다.** 같은 `0 3` 이 서버 시간대에 따라 새벽 3시가 되기도, 한낮이 되기도 합니다. **서버를 옮기면 크론 시간대부터 다시 봅니다** → [형제 시스템 히스토리 장면 ①](../DESIGN-HISTORY-SYSTEMS.md).
  - 등록 절차의 세 가지 순서: ① **손으로 한 번 돌려 되는 것을 확인한 뒤** 자동화한다 ② 등록 전에 **이미 등록돼 있는지 먼저 본다**(중복 등록이 실제로 있었습니다) ③ **복원까지 한 번 돌려 봅니다** — 백업 파일이 생기는 것과 그 파일로 되살아나는 것은 다른 사실입니다(S7).
  - 근거: HIS 기준 커밋의 백업 스크립트와 백업·키 교체 참조 문서 · 2026-09-12 확인.
- **Redis 메모리 한도와 축출 정책**은 compose 기본값이 **`maxmemory 512mb` · `allkeys-lru`** 입니다. 상한 없이(`noeviction`) 두면 세션·캐시가 쌓여 메모리가 터지고 **인증 전체가 멎기 때문에** 상한을 두었습니다. ⚠️ `allkeys-lru` 는 **만료 시간이 없는 세션 · 패스키 값도 밀어낼 수 있으므로**, 기관의 평상시 사용량보다 넉넉하게 올려 잡습니다(기관 규모별 값은 리허설에서 측정 · `확인 필요(따라가기)`).
- 한글 검색용 GIN 인덱스(시스템이 DB 를 조회해 판정) · PostgreSQL 튜닝을 개시 전에 맞춥니다. 권장 값은 `확인 필요(따라가기)`.

### 병원 규정

병원 규정 값은 코드가 아니라 **설정 화면**에서 바꿉니다. 어휘 · 수치 정책 · 상태 라벨은 한 곳(공용 정본)에서 나오므로, 규정을 바꿀 때 한 곳만 고칩니다. 안전 게이트(DUR · CDSS · 조영제 등)는 이 단계에서 존재를 확인하고, 목표 모드는 [S7](S7-rehearsal.md) 리허설에서 경고로 운영해 본 뒤 올립니다.

## ④ 사람이 정할 것 (20)

| 키 | 층 | 개시 관계 | 무엇을 정하나 |
|---|---|---|---|
| `billing.institutionType` | 허가권자 | 🔴 개시 전 필수 | 요양기관 종별(수가 단가 기준). 기록하면 시스템이 반영합니다 |
| `country.procedureCodeSystem` | 허가권자 | 개시 뒤 가능 | 처치 · 수가 코드 체계 이름을 무엇으로 적을 것인가 |
| `policy.catalogOwnership` | 원내 위원회 | 개시와 무관 | 카탈로그 소유와 분모 정의를 누가 맡을 것인가 |
| `codes.newCodeApproval` | 원내 위원회 | 개시 뒤 가능(30일) | 코드 마스터에 코드를 새로 만드는 것을 누가 승인하는가 |
| `pharmacy.prescriptionValidityBasis` | 원내 위원회 | 개시 뒤 가능 | 원외 처방전 유효기간의 근거, 설정으로 뺄 것인가(지금은 코드 값) |
| `pharmacy.splitDispensingSchema` | 원내 위원회 | 개시 뒤 가능(30일) | 분할조제 회차를 어디에 담을 것인가(배포로 반영) |
| `schema.pressureUlcer.reassessment` | 원내 위원회 | 개시 뒤 가능(30일) | 욕창(Braden) 재평가를 어디에 저장할 것인가(배포로 반영) |
| `draft.cacheTtl` | 원내 위원회 | 개시 뒤 가능(30일) | 작성 중 임시 보관(암호화 캐시)의 보관 시간 |
| `draft.cacheKeyScope` | 원내 위원회 | 개시 뒤 가능(30일) | 임시 보관을 환자 · 기록 단위로 나눌 것인가 |
| `draft.visibility` | 원내 위원회 | 개시 뒤 가능(30일) | 확정 전 초안을 누가 볼 수 있는가 |
| `draft.retention` | 원내 위원회 | 개시 뒤 가능(30일) | 확정되지 않은 초안을 언제까지 두는가 |
| `draft.certificatePrewrite` | 원내 위원회 | 개시 뒤 가능(30일) | 증명서를 환자 신청 전에 미리 작성해 둘 수 있게 할 것인가 |
| `draft.staffInitiatedFee` | 원내 위원회 | 개시 뒤 가능(30일) | 원내에서 먼저 만든 증명서에 수수료 · 처리기한을 적용할 것인가 |
| `draft.phraseSharing` | 원내 위원회 | 개시 뒤 가능(30일) | 개인 상용구를 진료과 공용으로 올리는 권한 |
| `draft.personalLayout` | 원내 위원회 | 개시 뒤 가능(30일) | 기록 화면 구성을 개인별로 바꿀 수 있는 범위 |
| `draft.sampleApproval` | 원내 위원회 | 개시 뒤 가능(30일) | 초안 견본을 누가 원내 표준으로 승인하는가 |
| `i18n.reviewOwnership` | 원내 위원회 | 개시 뒤 가능(30일) | 번역 검수를 누가 하는가(의료 용어와 일반 화면을 가를 것인가) |
| `i18n.safetyStringTranslation` | 원내 위원회 | 개시 뒤 가능(30일) | 안전 관련 문구(투약 확인 · 고위험 · 위급값 등)를 번역할 것인가 |
| `i18n.packActivationCriteria` | 원내 위원회 | 개시 뒤 가능(30일) | 번역 범위가 몇 %부터 언어를 선택지에 올릴 것인가 |
| `i18n.unwiredScreenScope` | 원내 위원회 | 개시 뒤 가능 | 번역 배선이 없는 화면을 어디까지 채울 것인가(배포로 반영) |

`draft.*` · `i18n.*` 는 개시 뒤 30일 안에 정해도 되지만, 리허설(S7)에서 실제 화면을 보며 정하는 편이 근거가 남습니다.

## ⑤ 확인

| 확인 화면 | 무엇을 보나 |
|---|---|
| `/admin/config` | 기관 정보 · 주소 · 암호화 키 설정의 출처(DB / 기본값 / 미설정) |
| `/admin/go-live` | 아래 Go-Live 항목의 실검증 결과 |
| `/admin/data-quality` | 기관 기본정보 입력 여부 |
| `/admin/billing-calc` | 요양기관 종별 설정 |
| `/admin/sentinel` | 상시 감시자가 떠 있는지 — 파이프 생존 축(백업 · 예약 작업 · 배포 최신성) |

> **새 설치본에서 본 값**(따라가기 2026-09-13 · 기본 시드 직후 · 리허설 모드) — `/admin/opening` 은 **모든 묶음 완료 0**이고 "기관 국가가 설정되지 않아 기본값으로 보고 있습니다(`hospital.country`)"를 먼저 띄웁니다. 위원회 7개는 "위원 0명 / 정족수 N"으로 미준비입니다. `/admin/go-live` 는 **전환 준비도 19%(필수 6/31) · 전환을 막는 항목 25건**입니다. 시드가 켠 연동 게이트도 "개발 시드" 표시로 남아 완료로 올라가지 않습니다 — **처음부터 아무것도 끝났다고 말하지 않습니다.**
>
> 🔴 **백업 판정(`security.dbBackup` · `open.system.backup`)은 새 설치본에서 늘 「판정 불가」입니다** — 백업 파일을 찾는 폴더가 코드에 고정돼 있어 기관 서버에는 없는 경로를 읽습니다. 0 이나 통과로 말하지 않는 것은 맞지만, 백업을 실제로 해도 이 항목은 스스로 닫히지 않습니다. 백업 확인은 [S8](S8-go-real.md) 복원 시험으로 따로 남깁니다.

이 단계를 닫는 개원 단계 항목 (8):

| 키 | 항목 | 확인 방식 |
|---|---|---|
| `open.system.hospitalInfo` | (한국) 기관 기본정보 확정 | 파생 — Go-Live `data.hospitalInfo` 를 따름 · 선행 `open.insurance.nhisCode` · `open.license.bizreg` |
| `open.ae.hospitalInfo` | (UAE) 기관 기본정보 확정 — 한국과 **다른 값**(면허번호 · 상업 면허) | 파생 — Go-Live `data.hospitalInfo` |
| `open.insurance.institutionType` | 요양기관 종별 확정 | 파생 — 결정 `billing.institutionType` |
| `open.system.backup` | 정기 DB 백업 실행 확인 | 파생 — Go-Live `security.dbBackup` |
| `open.gov.policies` | 원내 규정 · 지침 제정 | 사람 기록 |
| `open.workforce.licenses` | 의료인 · 의료기사 면허 확인 | 사람 기록 |
| `open.ae.staffLicenses` | (UAE) 관할 개인 면허 취득 | 사람 기록 |
| `open.facility.foodservice` | 급식 운영 형태 확정 — 급양 모듈 운영 설정과 이어집니다 | 사람 기록 |

이 단계를 닫는 Go-Live 항목 (8):

| 키 | 항목 | 판정 | 개시 차단 |
|---|---|---|---|
| `data.hospitalInfo` | 기관 기본정보 완비 | 실검증 | 🔴 예 |
| `billing.institutionGrade` | 요양기관 종별 설정(개설 허가와 일치) | 실검증 | 🔴 예 |
| `security.encryptionKey` | 암호화 키가 설정돼 있고 개발용 값이 아님 | 실검증 | 🔴 예 |
| `security.dbBackup` | 최근 암호화 백업 파일의 생성 시각 | 실검증 | 🔴 예 |
| `infra.ginIndex` | 한글 검색 GIN 인덱스가 DB 에 실제로 있는지 | 실검증 | 🔴 예 |
| `infra.redis` | Redis 메모리 한도 · 축출 정책 | 자가신고 | 🔴 예 |
| `infra.pgTuning` | PostgreSQL 튜닝 | 자가신고 | 아니오 |
| `flag.uiA11yDefaults` | 접근성 기본값(본문 배율 · 대비 · 색 외 상태 기호) | 자가신고 | 아니오 |

## ⑥ 아직 안 되는 것과 대체 수단

| 아직 안 되는 것 | 대체 수단 |
|---|---|
| 병원명이 설정값이 아니라 코드에 남은 파일이 있습니다(118개) | 이관이 끝날 때까지 코드를 고칩니다 |
| 대외 기관 전송(청구 · 자격조회 · 법정 보고) 모듈이 없습니다 | 기관이 전송 모듈을 붙이거나 기존 청구 소프트웨어와 함께 씁니다 → [S5](S5-management.md) · [S3](S3-clinical-departments.md) |
| 오더세트 금기 조건이 자동으로 평가되지 않습니다 | 화면이 "금기 수동 확인"으로 표시합니다. 사람이 확인합니다 |
| 원외 처방전 유효기간이 설정이 아니라 코드 값입니다 | 결정 `pharmacy.prescriptionValidityBasis` |
| KCD 표기가 제8차입니다(제9차 시행 중) | 반입 판본과 코드 표기를 대조합니다. 반영 계획은 통합 릴리즈에서 다시 확인합니다 |
| 화면 번역은 AI 초안이고, 서버가 조립하는 한국어 문장 일부는 번역되지 않습니다. 아랍어 오른쪽→왼쪽 화면은 준비 단계입니다 | 결정 `i18n.*` 로 검수 주체와 노출 기준을 정합니다 |
| 감시자의 흐름 완결성 축은 과거 표본이 쌓이기 전에 판정하지 않습니다 | "관측 불가"는 결함이 아니라 판정 보류입니다. 리허설 기간에 표본이 쌓입니다 |
| 오프라인 동기화는 범위 밖입니다 | 망 단절 시 운영 절차를 기관이 따로 정합니다 |
| 🔴 **저장소의 컨테이너 설치 파일(운영 compose · Dockerfile)이 그대로는 동작하지 않습니다**(따라가기 2026-09-13) — API · 웹 빌드가 Node 기본 메모리 한도에서 멈추고, 웹 이미지는 선언되지 않은 의존성 · 없는 standalone 산출물에서 멈추며, 마이그레이션 컨테이너는 스키마를 못 찾고, 운영 이미지에는 시드 도구가 없고, API 는 기본으로 `127.0.0.1` 에만 바인딩해 다른 컨테이너가 닿지 못합니다 | 따라가기에서 한 우회: 빌드에 `NODE_OPTIONS=--max-old-space-size=6144` · 웹 설치를 전체 워크스페이스로 · 웹 실행을 `next start` 로 · 마이그레이션 작업 폴더를 `apps/api` 로 · 시드는 개발 의존성을 가진 별도 이미지 · API 에 `BIND_HOST=0.0.0.0`(내부 네트워크 안에서만). **새 기관이 쓸 수 있는 유지되는 컨테이너 설치 경로는 아직 없습니다** |

## ⑦ 흔한 함정

- **시드를 넣은 채 격리를 푸는 것.** 기본 시드는 가상 병원이고 특정 기관명이 들어 있습니다. 주소 교체와 격리 확인이 먼저입니다.
- **데이터 손실을 허용하는 옵션으로 스키마를 밀어 넣는 것.** 저장소가 금지합니다. 스키마가 바뀌는 배포는 자동 백업을 거치는 경로를 씁니다.
- **저장소의 배포 스크립트를 그대로 쓰는 것.** 실행한 작업 디렉터리를 그대로 서버에 동기화하므로, 커밋하지 않은 변경도 함께 올라가고 두 곳에서 동시에 배포하면 서로를 덮어씁니다. 새 기관의 배포 경로는 `확인 필요(따라가기)`.
- **`HIS_MODE` 를 비워 두는 것.** 비어 있으면 **REAL 로 동작**합니다(기동 로그가 경고합니다). 리허설 서버는 `HIS_MODE=REHEARSAL` 을 명시하고, 웹 빌드에도 `NEXT_PUBLIC_HIS_MODE` 를 줍니다 — 비워 두면 테스트 계정 패널이 빠진 REAL 빌드가 됩니다(모르는 값이면 막는 기본값).
- **격리를 확인하지 않고 여는 것.** 따라가기에서 HIS API 는 기동 뒤 **스스로 기관 공개 사이트의 최신성을 확인하려고 밖으로 나갔습니다**(격리 네트워크가 막음 · 기동 로그 "공개 사이트 최신성 확인 차단"). 기관 정보의 공개 사이트 주소를 자기 기관 값으로 바꾸기 전에는 격리를 풀지 않습니다.
- **웹 빌드 인자 `PUBLIC_URL` 을 빠뜨리는 것.** 운영 compose 는 비어 있으면 **특정 설치본 주소**를 API 주소로 넣어 빌드합니다.
- **"기본값"을 정한 값으로 오해하는 것.** `/admin/config` 에서 "기본값"은 아무도 정하지 않은 값입니다.
- **요양기관 기호에 개발용 자리표시가 남는 것.** 기호 · 사업자번호 · 대표자는 인허가 뒤에 원무가 확정합니다. Go-Live `data.hospitalInfo` 가 판정합니다.
- **백업 비밀을 서버에만 두는 것.** 서버를 잃으면 백업을 풀 수 없습니다.
- **관측 도구(Grafana 등)를 고쳐서 외부에 여는 것.** AGPL 계열의 조건을 확인합니다([THIRD_PARTY.md §5](../THIRD_PARTY.md#5-구조--링크하지-않고-별도-컨테이너로-씁니다)).
