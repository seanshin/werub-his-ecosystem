# S7 리허설 — 가상 데이터로 전 흐름 시연 · 안전 게이트 경고 운영 · 감시자 판정

> ⚠️ 초안 — **새 설치본으로 한 번 따라가 본 결과를 반영했습니다**(2026-09-13~16 · 개발 PC 한 대 · arm64 · 8GB 가상 머신 · GPU 없음 · 외부로 나가지 못하는 네트워크). 남은 `확인 필요(따라가기)` 표시는 x86 · GPU 기준 장비나 사람 결정이 필요한 곳입니다. · 기준 2026-09-11 · [가이드 목차](README.md)

> **EN** — Stage S7 is the rehearsal: run the whole hospital on synthetic data before anything real exists, with outbound sending blocked by the operating mode rather than by configuration alone. This chapter lists what the follow-along actually exercised end to end, what the seeds create (only two systems create virtual patients), and how to watch the integrity monitors.

## ① 목적과 완료 조건

실제 개시와 같은 흐름을 **가상 병원 데이터**로 끝까지 돌려 봅니다. 실환자 데이터는 필요 없습니다. 리허설은 세 가지를 확인하는 자리입니다 — 흐름이 시스템 경계를 넘어 끝까지 가는가 · 안전 게이트가 현장에서 견딜 만한가 · 감시자가 무엇을 판정하고 무엇을 "관측 불가"로 두는가.

이 초안의 연결 상태에는 `검증됨` 이 27개 — 목록은 [따라가 본 결과](follow-along-2026-09.md#실제로-호출해-검증됨-을-붙인-연결) 한 곳에 있습니다. 나머지 상태는 코드 대조입니다. **리허설은 기관이 쓰는 연결마다 실제로 호출해 확인일을 붙이는 단계**이기도 합니다.

완료 조건:

- [ ] HIS 가 리허설 모드로 돌고 있다 — **화면 위쪽에 운영 모드 배지가 늘 보입니다**(`리허설 · 시드 데이터 · 대외 발신 차단`). 환자가 보는 [환자 포털](../screens/patient-app.md#환자-포털--앱이-아직-없을-때-환자가-들어오는-곳)에도 같은 띠가 뜹니다 — "실환자 데이터가 아닙니다. 문자 · 메일 · 기관 전송은 실제로 나가지 않습니다" (2026-09-12 화면 확인)
- [ ] 아래 **전 흐름 시나리오**를 가상 환자로 끝까지 돌렸고, 끊긴 곳을 기록했다
- [ ] 안전 게이트 8개를 경고 모드로 운영해 보고, 목표 모드로 올렸다
- [ ] 상시 감시자의 네 축 판정을 확인하고, "관측 불가"의 이유를 설명할 수 있다
- [ ] 기관이 쓰는 연결마다 호출 결과와 확인일을 기록했다
- [ ] 기관 규모의 부하에서 처리량 · 응답 시간을 **측정 방법 · 날짜와 함께** 기록했다
- [ ] 운영위원회가 리허설 결과를 검토하고 보완 사항을 확정했다

## ② 설치 — 리허설 모드

HIS 에는 운영 모드가 셋 있습니다 — **개발 / 리허설 / 리얼**. 개시 전에는 각종 단계를 눌러 볼 수 있어야 하고, 개시 뒤에는 그 시험용 통로가 원천적으로 없어야 하기 때문입니다.

리얼 전환 뒤에는 시드 로그인(시험 계정) 노출이 끝납니다(개원 단계 `open.system.realMode`).

- 모드의 차이는 **빌드에 들어갑니다.** Go-Live `security.hisMode` 는 빌드의 운영 모드 상한을 읽습니다. HIS 웹의 모드 키는 `NEXT_PUBLIC_HIS_MODE` 이고, 배포 스크립트(`infra/scripts/deploy.sh`)가 이 값을 만들어 넣습니다.

  ```sh
  ./infra/scripts/deploy.sh [--schema] [--seed] [--api] [--web] [--all] [--mode dev|rehearsal|real]
  ```

  - 🔴 **`--mode` 를 주면 그 값이 서버에 못박히고, 주지 않으면 서버의 현재 값을 그대로 이어받습니다.** 배포할 때마다 모드가 제멋대로 되돌아가지 않게 한 설계입니다 — 리허설 중에는 **모드를 주지 않고 배포**해도 리허설로 남습니다.
  - 모드 값이 들어가는 파일은 **스크립트가 만듭니다.** 파일 머리에 "손으로 고치지 말 것 — 다음 배포에 덮인다"고 적혀 있습니다.
  - `--mode` 를 주고 웹만 배포해도 **API 가 새 모드를 알도록** 스크립트가 따로 맞춥니다. 두 쪽이 서로 다른 모드를 믿는 상태를 막습니다.
  - ⚠️ 이 스크립트는 **실행한 작업 디렉터리를 서버에 동기화**합니다 → [S1 「하지 말 것」](S1-core-his.md).
  - 🔴 **이 스크립트는 기준 커밋 상태로는 다른 기관에 쓸 수 없습니다**(2026-09-17 기준 커밋에서 다시 확인). 대상 서버 · ssh 포트 · 원격 배포 디렉터리 · 백업 디렉터리 · 원격 Node 실행 경로가 **스크립트 본문에 값으로 박혀 있고**, 그것을 덮어쓸 설정 파일도 환경 변수도 없습니다. 밖에서 바꿀 수 있는 값은 **세 개뿐**입니다(홈페이지 API 주소 · 홈페이지 공개 주소 · 운영 모드). 그래서 새 기관은 **스크립트를 고쳐 씁니다**(바꿔야 할 코드 기본값 목록 → [S1](S1-core-his.md)).
    - ⚠️ 앞선 판에서 이 자리를 "환경 변수로 덮습니다"라고 적었던 것은 **기준 커밋이 아니라 이후 커밋을 읽은 것**이었습니다. 저장소에는 이 경로들을 설정 파일로 빼는 변경이 **기준 커밋 뒤에 들어와 있습니다** — 기준 커밋을 옮기면 이 문단도 함께 바뀝니다.
  - 배포 원천은 **로컬 작업 디렉터리 → rsync → 서버에서 빌드** 하나뿐입니다. 저장소에서 내려받아 배포하는 경로 · 지속적 통합 · 컨테이너 레지스트리 경로는 **없습니다.** 그래서 서버에 올라가는 것이 *어느 커밋인가*가 아니라 *누구의 작업 디렉터리인가*로 정해집니다 — 배포 담당을 한 사람으로 정하는 이유입니다.
  - 서버 쪽 전제: **Node 와 프로세스 관리자(pm2)가 미리 있어야 합니다.** 비대화형 ssh 는 이들을 경로에서 찾지 못하므로 스크립트가 Node 실행 경로를 따로 주입합니다 — 서버의 Node 설치 위치가 다르면 **그 값도 스크립트에서 함께 고칩니다.**
  - 근거: HIS 기준 커밋의 배포 스크립트 사용법 주석과 인자 처리(2026-09-12 확인).
- **실행 중 모드 표시**는 확인했습니다 — 직원 화면과 환자 포털 위쪽에 배지로 늘 떠 있습니다(2026-09-12). **전환은 화면에서 하는 일이 아닙니다**: Go-Live 관제의 `security.hisMode` 는 **빌드의 운영 모드 상한을 읽는 실검증 항목**이라 화면에서 표시만 해서는 닫히지 않고, 리얼 빌드를 배포해야 합니다 → [Go-Live 관제 화면](../screens/his.md#구축을-진행하는-세-화면) · [S8](S8-go-real.md). 배포 명령과 옵션은 바로 위에 있습니다.
- 다른 시스템의 리허설 데이터: LIS 는 시드 프로파일 `demo` 에 데모 환자 · 데모 계정이 있고, 파일럿 데이터 정리 API 는 요청 → 승인 두 단계입니다. ERP 시드는 합성 데이터(`TEST-` 접두)입니다. 나머지는 기준 커밋에 있는 시드 스크립트가 다릅니다 — PACS 는 기관 정보 · 판독 템플릿 시드(`seed_institution` · `seed_templates`), sign 은 개발용 · 직원 시드(`seed-dev` · `seed-staff`), edu 는 교육 콘텐츠 · 문항 · 온보딩 시드(Prisma 시드 여럿), cerno 는 기관 문서 등록 시드(`seed_org_docs`)이고, **twin 은 시드 스크립트가 없습니다**(가상 데이터는 HIS 에서 받아 파생).

  **가상 환자를 만드는 것은 둘뿐입니다**(2026-09-16 기준 커밋 확인).

  | 시스템 | 가상 환자 | 실행 인자 |
  |---|---|---|
  | LIS | ✅ 데모 프로파일이 **데모 계정 · 환자 · 오더 · 결과**를 만듭니다 | 환경 변수로 프로파일을 고릅니다(`starter` · `demo` · `all`). 🔴 **기본값이 `all` 이라 그냥 돌리면 데모 데이터까지 들어갑니다** — 개시용 최소 데이터만 원하면 `starter` 를 명시합니다 |
  | sign | ✅ 단 **진료 기록이 아니라 가상 환자 이름의 인증서**입니다(환자 4 · 의료진 · 입회 · 대리) | 인자를 받지 않습니다 |
  | PACS | ❌ 기관 정보 · 판독 템플릿만 | 인자를 받지 않습니다 |
  | cerno | ❌ 기관 문서(임상 지침 본문)만 | 인자를 받지 않습니다 |
  | edu | ❌ 교육 콘텐츠 · 문항 · 온보딩 | — |
  | ERP | ❌ 합성 거래 데이터(`TEST-` 접두) | — |
  | twin | ❌ 시드 스크립트가 없습니다 | — |

## ③ 설정

### 전 흐름 시나리오

한 가상 환자의 여정을 시스템 경계를 넘어 끝까지 따라갑니다([ROADMAP §3](../ROADMAP.md#3-관통-흐름--시스템-경계를-넘어-한-줄로-읽히는-것)). 데모 시나리오 원고는 [`scenarios/`](../scenarios/)에서 준비 중입니다.

| # | 흐름 | 지나가는 연결(상태) |
|---|---|---|
| 1 | 예약(홈페이지 · 앱) → 접수 | 공개 홈페이지 → HIS · 환자 앱 → HIS (`구현·미검증`) |
| 2 | 진료(AI 보조 — S6 에서 켠 기능만) | HIS → AI Server (`구현·미검증`) |
| 3 | 검사 오더 → 결과 | HIS ⇄ LIS · FHIR — 오더 전달 · 환자 조회 · 결과 전달 · 오더 취소 전파 · 검사코드 카탈로그 반입 `검증됨`(2026-09-14~15 · 아래 「따라가기에서 돌려 본 것」) · Reflex 추가검사 · 수혈 동의 확인은 `구현·미검증`(따라가기에서 완료 확인 못 함 → [S3](S3-clinical-departments.md)) |
| 4 | 병리 영상 | LIS → PACS 병리 워크리스트 · 병리 뷰어 링크 `검증됨`(2026-09-15) |
| 5 | 영상 오더 → 워크리스트 → 판독 | HIS → PACS · 장비 → PACS (`구현·미검증` — 🔴 따라가기에서 **HIS 화면에서 PACS 를 부르는 경로는 확인하지 못했습니다** → [S3](S3-clinical-departments.md). 판독은 PACS 화면에서 합니다) |
| 6 | 판독 서명 · 동의서 서명 | PACS → sign 판독 서명 · 조영제 동의서 환자 서명 `검증됨`(2026-09-15) · HIS → sign 직원 서명 · 완료 통지 · 오더 로그 봉인 `검증됨`(2026-09-14) |
| 7 | 수납 · 청구 → 회계 | 직원 SSO · 진료비 계산서 조회 · 청구 라인 · 재원 조회 · 약품 보험코드 매핑 · 검진권 정산 회신 · 의료진 계약 서명 발의 `검증됨`(2026-09-14~15) · LIS → ERP 검사 청구 `검증됨`(2026-09-15) · 🔴 **HIS 운영 이벤트 전달은 리얼 모드에서만 나갑니다**(리허설에서는 대기열에 보류 — [S8](S8-go-real.md)) · 대외 청구 전송은 없음 |
| 8 | 결과 열람(포털 · 앱) | 환자 포털 → PACS · 환자 앱 → HIS (`구현·미검증`) |
| 9 | 원격 상담 | HIS → Jitsi (`중단` — S2 에서 새로 구성한 경우만) |

직원 쪽 흐름도 한 번 돕니다 — 입사 등록 → Clinic · edu 반영 → 교육 이수 → 이수증 서명(edu → sign) → 퇴직 처리([S5](S5-management.md)). 따라가기에서는 **직원 명부 동기화 → 이수 → HIS 교육 기록 → 이수증 서명 · 완료 통지**까지 새 설치본끼리 확인했습니다(2026-09-14~15). 입사 · 퇴직 이벤트 웹훅은 HIS 리허설 모드에서 보내지 않아 확인하지 못했습니다.

### 따라가기에서 돌려 본 것 (2026-09-15 · 새 설치본끼리 · 격리 네트워크 · 가상 환자)

한 대의 8GB 가상 머신에 HIS · LIS · AI Server 를 함께 올려 **검사 흐름 한 줄**을 끝까지 돌렸습니다.

| 단계 | 누가 · 어디서 | 결과 |
|---|---|---|
| 검사 처방(TSH) | HIS | 오더 생성 → LIS 가 5분 주기 폴링으로 가져감 |
| 검체 접수 | LIS 접수 역할 | 접수 · 바코드. **LIS 관리자 역할은 접수 · 결과 입력을 할 수 없습니다**(403 — 직무 분리). 새로 만든 LIS 계정은 **첫 로그인에서 비밀번호를 바꿔야** 다른 기능이 열립니다 |
| 결과 입력 | LIS 검사 역할 | 기준 범위 안 값 → 자동 검증 → HIS 로 결과 자동 발신 |
| 결과 수신 · 적용 | HIS 검사실 `파일 수신` 탭(검사 역할) | 오더와 자동 결속(MATCHED) → `결과 적용` |
| 검증 · 확정 | HIS 검사실 `검증 대기` 탭(다른 직원) | **적용한 직원 본인은 검증할 수 없습니다**(독립 이중검증) → 다른 직원이 검증 · 확정 |
| 오더 취소 | HIS 처방 취소 | 다음 폴링(따라가기 약 4분 30초) 뒤 LIS 오더도 취소 |

- 같은 결과를 다시 보내면 HIS 가 **중복**으로 막고, 없는 오더를 가리키는 결과는 **반영하지 않고 대기**에 둡니다(따라가기에서 일부러 보내 확인).
- HIS 는 결과를 적용할 때 **자기 기준치로 이상 여부를 다시 계산**합니다. HIS 에 그 검사의 기준치가 없으면 LIS 가 "정상"으로 보낸 값도 HIS 에서는 표시가 비어 있습니다 — 리허설에서 쓰는 검사마다 HIS 기준치를 확인합니다.
- LIS 는 이미 자동 검증한 결과여도, HIS 에서는 적용한 직원이 입력자로 기록돼 **다시 이중검증**을 거칩니다. 현장 동선(누가 적용하고 누가 확정하나)을 리허설에서 정합니다.
- 🔴 **LIS 설치 스크립트가 관리자 초기 비밀번호를 한 번만 출력**합니다. 설치 담당이 그 자리에서 기관 비밀 보관소로 옮기지 않으면 되찾을 방법이 없습니다(따라가기에서는 DB 에서 새 값으로 바꿨습니다).

### 연결 확인 기록

리허설에서 호출한 연결은 기관 문서에 다음 칸으로 남깁니다. 이 기록이 모이면 [연결 상태](../RELEASES/2026.09/compatibility.md)의 `구현·미검증` 을 `검증됨`(확인일)으로 올리는 근거가 됩니다.

| 연결(방향 · 목적) | 확인일 | 확인 방법 | 결과 | 확인한 사람(역할) |
|---|---|---|---|---|

### 안전 게이트 — 경고로 운영해 보고 올리기

안전 게이트(`/admin/safety-gates`)는 **끔 / 경고 / 차단** 세 모드입니다. 리허설에서는 먼저 경고로 운영하며 경고가 현장에서 어떻게 나오는지 보고, 목표 모드로 올립니다.

관제 화면에는 게이트가 **28개** 있습니다(2026-09-15 화면 확인). 아래 8개는 Go-Live 관제가 **목표 모드를 정해 둔 것**입니다.

따라가기에서 `dur.enforce` 를 **차단 → 경고 → 차단**으로 바꿔 가며 가상 환자에게 병용금기 약을 처방했습니다 — 차단에서는 처방이 거부되고, 경고에서는 경고와 함께 들어가며, 두 경우 모두 관제 화면의 위반 통계에 바로 잡혔습니다. 모드는 저장 즉시 반영됩니다. 🔴 **강제 처방(override) 을 쓸 수 있는 역할 · 사유 입력이 기관 규정과 맞는지** 리허설에서 반드시 직접 확인합니다.

| 게이트 키 | 무엇 | Go-Live 목표 모드 |
|---|---|---|
| `dur.enforce` | DUR 처방 게이트(병용금기 · 임부 · 알레르기 · 용량) | 차단 |
| `pharmacy.durGate.enforce` | DUR 조제 게이트(미해결 경고 조제 차단) | 경고 이상 |
| `cdss.enforce` | CDSS 처방 검증(알레르기 · 신기능 · 연령) | 경고 이상 |
| `imaging.contrastEnforce` | 조영제 안전(알레르기 · eGFR) | 경고 이상 |
| `imaging.pregnancyEnforce` | 임신 확인(가임기 전리방사선) | 경고 이상 |
| `inventory.expiryIssueEnforce` | 만료 · 리콜 LOT 불출 차단 | 경고 이상 |
| `formulary.enforce` | 원내 처방집 시행 | 경고 이상 |
| `psychiatry.ectConsentEnforce` | ECT 동의 게이트 | 차단 |

### 감시자 판정

상시 감시자(`/admin/sentinel`)는 **API 와 다른 별도 프로세스**로 네 축을 봅니다. 🔴 **HIS 운영 compose 에는 감시자가 없습니다** — 배포 스크립트가 API 빌드 결과의 `apps/api/dist/sentinel.main.js` 를 따로 띄웁니다. 띄우지 않으면 화면이 "스윕이 한 번도 실행되지 않았습니다 · 감시자 정지 의심"으로 **정직하게** 알려 줍니다(따라가기: API 와 같은 이미지 · 같은 환경변수로 컨테이너 하나를 더 띄우자 기동 즉시 네 축 스윕 — 데이터 정합성 불변식 71개 평가 · 흐름 · 계약 · 파이프, 1분 실시간 지표는 2시간 기준선이 쌓일 때까지 판정 보류). 네 축은 **데이터 정합성 · 파이프 생존(백업 · 예약 작업 · 배포 최신성) · 연동 계약 어긋남 · 흐름 완결성**, 그리고 1분 단위 실시간 지표. 판정하지 못한 것은 0 이 아니라 **"관측 불가"** 로 표시하고, 새로 생기거나 다시 생긴 문제만 관리자에게 알립니다. 흐름 완결성 축은 과거 표본이 쌓이기 전에는 판정하지 않으므로, 리허설 기간이 그 표본을 쌓는 기간입니다. 감시자 알림은 설정 세 개로 다룹니다(2026-09-16 기준 커밋 확인). **셋 다 원내 관리자 화면 안에서 끝나고 대외로 나가지 않습니다.**

| 설정 | 기본값 | 무엇을 정하나 |
|---|---|---|
| 신규 발견 알림 | 켜짐 | 10분 간격으로 **새로 생기거나 다시 생긴 것만** 알립니다. 이미 알린 항목을 다시 알리지 않습니다 |
| 알릴 심각도 | 적색만 | 주황 · 참고를 넣으면 알림이 늘어납니다. 🔴 **「관측 불가」는 어떤 설정에서도 알리지 않습니다** — 확인하지 못한 것으로 사람을 깨우지 않습니다 |
| 주간 요약 | 켜짐 | 월요일 아침에 현재 상태를 **신규 여부와 무관하게** 한 번 정리해 보냅니다 |

발견 항목은 고치는 조치가 어디서 일어나는지에 따라 **관리자 · 의료진 · 행정**으로 갈라 보여 줍니다. 🔴 이것은 **보여 주는 분류이지 권한이 아닙니다** — 화면 자체는 관리자만 봅니다.

### 대외 발신

리허설 중에도 **외부로 나가는 연결은 격리를 유지**합니다.

**채널별 상태는 제품 화면에서 봅니다**(2026-09-12 확인).

| 화면 | 무엇을 보여 주나 |
|---|---|
| [대외 발신 관제](../screens/his.md#대외-발신-관제--구현--설정--승인) | 환자 앱 푸시 · 문자 등 **통로별로 구현 여부(코드 선언)와 켜짐 여부(운영 설정)를 나눠** 보여 줍니다. 이 리허설 설치본은 "통로 6개 중 실제 발신 2개" |
| [기관 연동](../screens/his.md#기관-연동--안-되는-것을-일정으로-관리한다) | 법정 보고 · 청구 채널 7개의 전송 구현 여부와 **그동안의 수기 대체 절차** |
| 환자 포털 배지 | "문자 · 메일 · 기관 전송은 실제로 나가지 않습니다" |

**각 채널이 리허설 모드 때문에 막힌 것인지, 애초에 구현이 없어 안 나가는 것인지**는 위 두 화면이 구분해 보여 줍니다. 그래도 **확인이 끝나기 전에는 네트워크 격리로 이중으로 막습니다.**

### 성능 계측

권장 사양과 처리량은 아직 계측이 없습니다. 리허설에서 기관 규모의 부하로 측정하고, **값 · 측정 방법 · 측정일 · 장비 구성**을 함께 적습니다.

**부하 시험 도구는 13개 중 PACS 한 곳에만 있습니다**(2026-09-16 기준 커밋 확인). 시나리오는 워크리스트 · 통계 · 대시보드 · 판독 보고서 · 알림 · 영상 검색과 조회 · 영상 업로드 · 실시간 연결이고, **가벼운 확인(5명 · 1분)과 개시 전 확인(50명 · 5분) 두 벌**로 돌리게 되어 있습니다. 🔴 다만 **대상 주소가 스크립트에 고정돼 있으므로 자기 설치본을 가리키도록 고친 뒤 씁니다.**

나머지 시스템에는 부하 시험 도구가 없습니다. 기관이 직접 시나리오를 만들어야 하며, **무엇을 재고 무엇을 성공으로 볼지**는 기관이 정합니다 — 이 자료는 재 보지 않은 값을 권장 사양으로 적지 않습니다.

> **따라가기에서 잰 것**(2026-09-15 · 가상 데이터 · 사용자 없음 · 기준 장비 아님) — HIS(+감시자) · ERP · PACS 코어 · AI Server(작은 대체 모델)를 **함께 올렸을 때 가상 머신 메모리 사용이 약 2.9GB**(컨테이너 사용량 합계 · `docker stats` · 8GB 중)였습니다. 부하를 준 값이 아니므로 **사양 산정 근거로 쓰지 않습니다.** 🔴 먼저 모자란 것은 메모리가 아니라 **디스크**였습니다 — 60GB 가상 머신이 빌드 이미지와 모델로 두 번 가득 찼고, 그때 같은 호스트의 DB 가 복구 모드로 들어갔습니다. 리허설 장비는 **디스크를 200GB 이상**으로 잡고, 옛 빌드 이미지를 정리하는 절차를 함께 둡니다.

## ④ 사람이 정할 것 (3)

| 키 | 층 | 개시 관계 | 무엇을 정하나 |
|---|---|---|---|
| `policy.alertRouting` | 원내 위원회 | 개시 뒤 가능(30일) | 경보 라우팅표 · 단계(tier)를 승인할 것인가 |
| `qi.waitThresholdUnify` | 원내 위원회 | 개시 뒤 가능(30일) | "너무 오래 기다렸다"의 기준을 하나로 할 것인가(관제 30분 · 현장 20/60분) |
| `qi.measurementSource` | 원내 위원회 | 개시 뒤 가능(30일) | 질지표 측정값의 출처(자동 산출 · 수기 입력)를 원장에 남길 것인가 |

셋 모두 리허설에서 실제 경보 · 대기 · 지표를 보며 정하는 편이 근거가 남습니다. [S1](S1-core-his.md)의 `draft.*` · `i18n.*` 결정도 이 기간에 화면을 보며 정하기 좋습니다.

## ⑤ 확인

| 확인 화면 | 무엇을 보나 |
|---|---|
| `/admin/sentinel` | 네 축 판정 · 관측 불가의 이유 |
| `/admin/safety-gates` | 게이트별 현재 모드 |
| `/admin/go-live` | 준비율 — 실검증과 자가신고를 따로 셉니다 |
| `/admin/opening` | 리허설 항목 |
| `/admin/outbound-channels` | 채널별 대외 발신 상태 |
| `/admin/integration-gates` | 연동 게이트 결재 기록 |

이 단계를 닫는 개원 단계 항목 (2):

| 키 | 항목 | 확인 방식 |
|---|---|---|
| `open.rehearsal.run` | 리허설 운영(실제와 같은 흐름으로 돌려 본다) | 사람 기록 |
| `open.rehearsal.review` | 리허설 결과 검토 · 보완 사항 확정 | 파생 — 운영위원회(`OPS`) 명부로 판정 |

이 단계를 닫는 Go-Live 항목 (8) — `A` 임상 안전게이트, 모두 실검증 · 🔴 개시 차단:

`gate.dur.enforce` · `gate.pharmacy.durGate` · `gate.cdss.enforce` · `gate.imaging.contrast` · `gate.imaging.pregnancy` · `gate.inventory.expiry` · `gate.formulary.enforce` · `gate.psychiatry.ect`

## ⑥ 아직 안 되는 것과 대체 수단

| 아직 안 되는 것 | 대체 수단 |
|---|---|
| 대외 기관 전송(청구 · 자격조회 · 법정 보고)이 없습니다 | 리허설의 청구 · 보고 흐름은 "작성까지"로 돌리고, 대체 경로(S3 · S5)를 따로 연습합니다 |
| 본인확인 문자가 모의 발송입니다 | 모의 발송임을 화면에서 확인하고 넘어갑니다 |
| 원격 화상이 `중단` 입니다 | Jitsi 를 새로 구성하지 않았다면 9번 흐름은 건너뛰고 그 사실을 적습니다 |
| 연결 113개 중 `검증됨` 은 27개입니다 | 리허설 호출 기록이 나머지의 근거가 됩니다 |
| 데모 시나리오 원고가 준비 중입니다 | 위 표의 흐름으로 기관이 시나리오를 짭니다 |
| 감시자 흐름 완결성 축은 표본 전에는 판정하지 않습니다 | 리허설 기간을 표본 기간으로 씁니다 |

## ⑦ 흔한 함정

- **리허설에 실환자 정보를 넣는 것.** 리허설은 가상 데이터로 합니다. 실데이터는 [S8](S8-go-real.md)의 격리 · 이관 뒤에 들어옵니다.
- **게이트를 처음부터 차단으로 켜는 것.** 경고가 어떻게 나오는지 보지 않고 차단하면 현장이 우회로를 찾습니다.
- **"관측 불가"를 "문제 없음"으로 읽는 것.** 판정하지 못했다는 뜻입니다.
- **자가신고 항목을 실검증처럼 세는 것.** Go-Live 준비율은 둘을 따로 셉니다. 자가신고의 근거는 화면 밖에 있습니다.
- **파생 항목에 완료를 적으려는 것.** 파생 항목은 다른 정본(Go-Live · 결정 등록부 · 위원회 명부)의 상태를 그대로 보여 줍니다.
- **"설정을 켰다"를 "한 바퀴 돌았다"로 착각하는 것.** 흐름마다 끝까지 한 건을 돌려야 확인입니다.
- **리허설 결과를 기록하지 않는 것.** 확인일 없는 확인은 발행 자료에서 `검증됨`이 될 수 없습니다.
