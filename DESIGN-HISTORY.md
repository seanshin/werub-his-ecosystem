# 설계 기준은 어떻게 생겼나 — 코어 HIS 의 작업 히스토리

**How the design rules came about — a history of the core HIS**

> **EN** — This ecosystem's core HIS went through **108 releases** (v3.11 → v4.19, 2026-06 ~ 2026-09). The first half built features; the second half spent release after release discovering that **"we built it" and "it works" are different facts**. The nine judgment rules that govern this ecosystem were not designed up front — **every one of them came from something that actually went wrong**, and they are the reason these materials say `0 verified`, print denominators next to ratios, and refuse to call an unmeasured zero "safe".

> 🟡 **초안** — 코어 HIS 저장소의 릴리즈 기록과 규칙 문서를 읽어 정리했습니다(2026-09-12). 소스는 공개하지 않으므로 **인용한 문장과 수치의 원문은 각 시스템 저장소에 있습니다.** 시스템 담당 확인 전입니다.

---

## 왜 이 문서가 있나

[개요서 2장](overview/02-principles.md)은 이 생태계의 여덟 가지 취지를 적습니다. 그런데 **그 원칙이 어디서 왔는지**는 적지 않습니다.

답은 간단합니다. **전부 겪은 것에서 나왔습니다.** 코어 HIS 저장소의 규칙 문서가 그렇게 밝힙니다.

> "아래는 전부 이 저장소에서 **실제로 겪은 것**에서만 나온 규칙이고, 신규 기능에도 그대로 적용한다."

이 문서는 그 과정을 정리한 것입니다. 구축을 앞둔 기관이 **"이 원칙들이 왜 이렇게 까다로운가"** 를 이해하는 데 쓰입니다.

---

## 두 시기

코어 HIS 는 2026-06 부터 2026-09 까지 **릴리즈 108개**(v3.11.0 → v4.19.0)를 냈습니다. 릴리즈 제목만 늘어놓아도 두 시기가 갈립니다.

### 1기 — 만들었다 (v3.11 ~ v3.97)

만성질환 관리 · 환자 여정 프로토콜 · 임상차트 아키타입 · 진단검사 보드 · 약가 전량 · 처방 UX · 약사 승인제 · 오더 시행 추적 · LIS Reflex 양방향 · 직원 전자결재 · 그룹웨어 동기화 · 응급실 · 고객의 소리 · 입력 검증 전수 · VoiceEMR · 외부 임상 앱 개방 · 거버넌스 · 수치 정책 · FHIR 출력 정합 …

**기능을 붙여 나가는 시기**입니다. 화면과 API 가 늘어납니다.

### 2기 — 만든 것이 실제로 하는지 확인했다 (v4.1 ~ v4.19)

2기의 릴리즈 **제목이 그대로 서사**입니다. 순서대로 읽으면 무엇을 배웠는지가 보입니다.

| 버전 | 제목 |
|---|---|
| v4.1 | 이연 회수 축 — 앞선 작업들이 남긴 것 |
| v4.5 | **BE 는 있는데 화면이 부르지 않던 안전장치들** |
| v4.8 | **켜는 것과 끄는 것이 같은 스위치가 아니었다** |
| v4.9 | **화면은 있었고, 그 화면이 하는 일이 없었다** |
| v4.10 | **정해도 아무 일이 일어나지 않았고, 정한 것보다 코드가 우선했다** |
| v4.11 | **정해야 할 것이 어디에도 이름을 갖고 있지 않았다** |
| v4.12 | **만든 것과 소비처가 쓴 것은 다른 일이었고, 대개 소비처가 죽어 있었다** |
| v4.13 | 검증은 파이프 밖에서도 새고, **사람 결정은 화면에서 이뤄져야 한다** |
| v4.14 | **시스템이 자기 자신에 대해 하는 말을 감시한다** |
| v4.15 | 관측점을 늘리자 **죽은 기능들이 걸려 나왔다** |
| v4.16 | 결함은 에이전트 **안**이 아니라 **사이**에 있었다 |
| v4.17 | **문서에 있는 동안에는 아무도 고를 수 없다** |
| v4.18 | **정본은 있는데 그 길을 아무도 안 지나갔다** |
| v4.19 | 평문으로 남던 백업, 9시간 이른 시각, 그리고 아무도 못 읽던 화면 |

**열세 번의 릴리즈가 거의 같은 말을 합니다** — 만든 것이 실제로는 그 일을 하지 않고 있었다는 것.

---

## 무엇을 배웠나 — 다섯 장면

### 1. "만들었다"와 "동작한다"는 다른 일이다 (v4.12)

> "이번 세션의 결함은 대부분 **개통 이래 한 번도 작동한 적 없던 것**이었고, 한 발견이 다음 발견을 여는 사슬로 드러났다."

서버에 쓰기 API 가 있는데 화면이 부르지 않거나, 화면이 부르는데 서버가 받지 않았습니다. 타입 검사도 테스트도 내내 통과하고 있었습니다.

→ 이 자료가 연결 상태를 **`구현·미검증`** 이라고 적고, 새 설치본에서 실제로 호출해 본 것에만 **`검증됨`** 을 붙이는(2026-09-15 현재 20개) 이유입니다([연결 상태](RELEASES/draft/compatibility.md)). "양쪽 코드가 맞물려 있다"와 "실제로 오간다"를 같은 말로 쓰지 않습니다.

### 2. 사람이 눌러 보면 나온다 (v4.9)

> "**사람이 화면을 하나씩 눌러 '이건 안 되는데요'를 8번 말했고, 그 8번이 전부 사실이었다.**"

→ 이 자료가 [구축 가이드](build-guide/)를 **새 설치본으로 따라가 본 뒤에야** 고치고, 확인하지 못한 절차를 `확인 필요(따라가기)` 로 남겨 둔 이유입니다. 한 번 따라가 보니(2026-09-13~15) 저장소의 운영 설치 파일이 **그대로는 동작하지 않는 곳**이 여럿 나왔습니다 — 읽어서 쓴 절차는 발행본이 아닙니다.

### 3. 자기가 하는 말을 감시하는 장치를 따로 두었다 (v4.14)

> "데이터 값의 이상은 이미 봤지만 **'지금 보고 있는 숫자를 믿어도 되는가'** 는 아무도 안 봤다. 그 질문을 던지는 **별도 프로세스 감시자**를 만들고, 감시자가 켜지자마자 나온 실결함들을 회수했다. **감시자 자신이 틀린 수를 말한 것까지 잡았다.**"

→ [상시 감시](screens/his.md#상시-감시--고치지-않고-사람에게-넘긴다) 화면이 그 결과물입니다. 감시자는 **데이터를 고치지 않고** 사람에게 넘기며, 판정하지 못한 것은 0 이 아니라 **"관측 불가"** 로 둡니다.

### 4. 결정은 문서가 아니라 화면에 있어야 한다 (v4.17)

> "🔴 **결정이 문서에 있는 동안에는 아무도 고를 수 없고, 미결이라는 사실이 화면 어디에도 없다.**"

→ [결정 등록부](checklist/decisions.md)와 [중요 결정 화면](screens/his.md#구축을-진행하는-세-화면)이 여기서 나왔습니다. 사람이 정할 것을 **층으로 나누고 화면에 띄워** 미결이 보이게 합니다 → [취지 5](overview/02-principles.md#5-사람이-정할-것은-화면에서)

### 5. 병렬로 밀면 결함이 "사이"에 생긴다 (v4.16)

> "개별 에이전트의 작업은 전부 검증을 통과했고 테스트는 내내 초록이었다 — **결함은 각 작업 안이 아니라 작업 사이에 있었다.**"

→ 이 자료가 **문서끼리 같은 사실을 말하는지 기계로 대조**하는 이유입니다(`check-consistency`). 각 문서가 옳아도 서로 어긋날 수 있습니다.

---

## 거기서 나온 판정 규칙 아홉 가지

코어 HIS 저장소는 겪은 것을 **아홉 축의 규칙**으로 묶어 두고, 신규 기능에도 그대로 적용합니다. 이 생태계 전체가 이 규칙 위에 있습니다.

| # | 축 | 한 줄 |
|---|---|---|
| 1 | **정직성** | 모르는 것을 아는 척하지 않는다 — **표본 부재의 정답은 0 이 아니라 "산출 불가"** |
| 2 | **정본** | 만든 것과 **모든 소비처가 쓰는 것**은 다른 일이다 |
| 3 | **가드·테스트** | 통과가 보증이 아니다 — **가드는 결함을 재주입해 실패해야 가드다** |
| 4 | **게이트·강제** | 막는 것과 방치되는 것 — **막는다면 그 기록이 서버에 남아야 한다** |
| 5 | **권한** | 뒤집힌 채 오래 산다 — **최소 권한 계정으로 실제 로그인해 훑는다** |
| 6 | **대외 발신·사람 결정** | 발신은 **구현 + 설정(기본 꺼짐) + 승인** · 결정 세 층은 고정, 하위가 상위를 대체하지 못한다 |
| 7 | **수치·날짜·소급** | **일어난 사실은 소급하지 않는다** · 비율에는 분모를 함께 |
| 8 | **빌드·배포·검증 층** | 타입 0 · 테스트 초록이 **배포 가능을 보장하지 않는다** · 운영 모드는 빌드가 상한 |
| 9 | **일하는 방식** | 무엇을 믿고 무엇을 눌러 보는가 — **실측이 기획을 이긴다** |

### 특히 되풀이되는 것 셋

**"가드는 결함을 재주입해 실패해야 가드다"**(3축)
> 통과하는 테스트가 아무것도 보증하지 않을 수 있다. **다른 이유로 통과한 사례**가 거듭 나왔습니다 — 픽스처가 분기를 못 겨누거나, 다른 쪽 쓰기 경로로 빠지거나, `import` 문이 사용 검사를 대신 통과하거나, 주석이 문자열 검사에 걸리거나.
> → 이 자료의 검사기들도 **자기 검증**(`--self-test`)을 먼저 통과해야 합니다. 공개 검사기는 18항목의 대조군을 함께 돌립니다.

**"실패를 없음으로 번역하지 않는다"**(1축)
> `.catch(() => 기본값)` 은 조회 실패를 "데이터 없음"으로 바꿔 말하는 코드입니다. 그러면 화면의 오류 표시가 **도달 불가 코드**가 됩니다.
> → 화면이 [아홉 가지 방식](overview/02-principles.md#화면이-모른다고-말하는-아홉-가지-방식)으로 "모른다"고 말하는 것이 여기서 나왔습니다.

**"숨긴 것과 없는 것은 다르다"**(8축)
> 조건부 렌더로 감춰도 **값은 번들에 남습니다.** 그래서 리얼 전환은 설정 토글이 아니라 **재배포**입니다.
> → [취지 8](overview/02-principles.md#8-개시-전과-후를-모드로-가른다)과 Go-Live 의 `security.hisMode`(빌드의 운영 모드 상한을 읽는 실검증 항목)가 여기서 나왔습니다.

---

## 이 규칙이 이 자료에도 적용됩니다

이 저장소는 소프트웨어가 아니라 **문서**지만, 같은 규칙을 씁니다.

| 규칙 | 이 자료에서 |
|---|---|
| 모르는 것을 아는 척하지 않는다 | **`검증됨` 20 / 113** 을 그대로 적습니다 · 계측하지 않은 것을 "충분하다"고 쓰지 않습니다 · 수치마다 **값 · 센 방법 · 계측일** |
| 정본은 하나 | 버전 · 규모 · 연결 상태는 [통합 릴리즈 매니페스트](RELEASES/draft/manifest.md) 한 곳에서 나옵니다. 저장소 README 의 수치를 옮겨 적지 않습니다 |
| 어긋나면 한쪽을 고르지 않는다 | 태그 · 문서 · 코드의 버전이 다르면 **어긋남으로 적습니다.** 2026-09-12 에 화면에서 그 값이 그대로 확인됐습니다([screens](screens/)) |
| 가드는 재주입해 증명 | 검사 14개를 `verify-all` 하나로 돌리고, 생성물은 `--check` 로 원본과 대조합니다. 통과하지 못하면 **푸시하지 않습니다** |
| 분모를 함께 | 연결 113개 중 상태별 수 · 캡처 75/266 · 체크리스트 56·60·60 — 항상 분모와 함께 |
| 사람이 확인할 것은 사람이 | 이미지는 글자 검사로 볼 수 없으므로, **사람이 캡처 대장에 확인 표시를 한 것만** 공개합니다 |

---

## 이 문서가 말하지 않는 것

- **이 문서는 코어 HIS 의 것입니다.** 나머지 12개 시스템의 히스토리는 따로 정리했습니다 → [형제 시스템은 어떻게 자랐나](DESIGN-HISTORY-SYSTEMS.md)(기록 666건 · 형식 4가지 · 되풀이된 장면 다섯).
- **인용한 문장의 원문은 비공개 저장소에 있습니다.** 이 자료는 소스를 담지 않으므로, 여기 옮긴 것은 **문장과 그 취지**입니다. 각 규칙의 근거(축 · 실측 수치 · 릴리즈 링크)는 코어 HIS 저장소의 규칙 참조 문서에 있습니다.
- **릴리즈 수(108)와 버전 범위(v3.11.0 ~ v4.19.0)는 2026-09-12 기준**입니다. 이 자료가 설명하는 기준 버전은 **v4.18.0** 이고([매니페스트](RELEASES/draft/manifest.md)), 그 뒤로 릴리즈가 더 나왔습니다.
- 이 문서는 **품질을 보증하지 않습니다.** "이런 규칙으로 일했다"는 사실을 옮길 뿐이며, 규칙이 있다고 결함이 없다는 뜻이 아닙니다 — 오히려 위 서사는 **결함이 거듭 나왔다**는 기록입니다.
