# ERP → Clinic

> 연동 계약 카드 — [카드 목록](README.md) · [연동 지도](../README.md) · 상태의 정본은 [연결 상태 표](../../RELEASES/draft/compatibility.md)입니다.

> **EN** — Integration contract card: **ERP → Clinic**. None of these 2 have been called end to end yet — they are wired in code only. Config keys are listed by **name only** — values are generated by the institution. Authoritative status lives in the [connection table](../../RELEASES/draft/compatibility.md).

**무엇을 주고받나** — `확인 필요`: 이 쌍을 한 문단으로 설명하는 글이 아직 없습니다.

## 연결

| 목적 | 프로토콜 | 인증 | 상태 | 확인일 |
|---|---|---|---|---|
| 그룹웨어 직원 명단 수집(ERP 인사 대사·clinicUserId 매핑) | HTTP GET /api/clinic/his/staff (일 1회 03:30) | API 키 Authorization: Bearer + X-Hospital-Code  | `구현·미검증` | — |
| 그룹웨어 근태(출퇴근) 수집 → 월 근태 집계 | HTTP GET /api/clinic/his/attendance?since= (일  | API 키 Authorization: Bearer + X-Hospital-Code | `미구현` | — |

## 양쪽에 넣는 설정 — **키 이름만**

값은 기관이 새로 만듭니다. 이 자료는 값을 담지 않습니다.

- `erp: clinic_api_base`
- `erp: clinic_api_key`
- `erp: hospital_code`
- `clinic: HIS API 키(관리 화면 발급)`

## 따라가기에서 확인한 것

이 방향은 **아직 실제로 불러 보지 않았습니다.** 상태는 양쪽 코드를 대조한 판정입니다.

