# HIS → Clinic · Jitsi

> 연동 계약 카드 — [카드 목록](README.md) · [연동 지도](../README.md) · 상태의 정본은 [연결 상태 표](../../RELEASES/draft/compatibility.md)입니다.

> **EN** — Integration contract card: **HIS → Clinic · Jitsi**. None of these 1 have been called end to end yet — they are wired in code only. Config keys are listed by **name only** — values are generated by the institution. Authoritative status lives in the [connection table](../../RELEASES/draft/compatibility.md).

**무엇을 주고받나** — HIS 안에 **작은 신원 발급기**를 두어 그룹웨어와 화상 서버에 쓸 단기 토큰을 한곳에서 내주자는 설계입니다. 지금은 규격만 있고, 두 시스템 어느 쪽도 이 방식으로 붙어 있지 않습니다.

## 연결

| 목적 | 프로토콜 | 인증 | 상태 | 확인일 |
|---|---|---|---|---|
| HIS 내부 IdP 단기 토큰(audience 'clinic'·'jitsi' · 경로 B redemption code 백채널 교환 · introspect) | REST `/api/v1/sso/internal/{grant,redeem,intro | 독립 서명키 HS 계열(AUTH_SSO_SIGNING_KEY · 없으면 AUTH_S | `설계만` | — |

## 양쪽에 넣는 설정 — **키 이름만**

값은 기관이 새로 만듭니다. 이 자료는 값을 담지 않습니다.

- `AUTH_SSO_SIGNING_KEY`
- `SSO_INTROSPECT_KEY`
- `AUTH_SECRET`

## 여는 순서

1. 지금 붙일 것이 아닙니다 — **설계 단계**입니다. 그룹웨어 · 화상 진입은 각 카드에 적힌 현재 방식을 보십시오.
2. 이 방식으로 갈지는 두 프로젝트와의 협의 사항입니다. 도입한다면 **서명키를 따로 두는 것**이 전제입니다(공용 비밀에서 파생시키지 않습니다).

## 따라가기에서 확인한 것

이 방향은 **아직 실제로 불러 보지 않았습니다.** 상태는 양쪽 코드를 대조한 판정입니다.

